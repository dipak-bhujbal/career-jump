import { describe, it } from "vitest";

describe("e2e run flow", () => {
  it.todo("triggers a manual scan run from the dashboard and shows a progress indicator");
  it.todo("displays the returned job matches after the run completes");
  it.todo("shows an error banner when a run is already in progress");
  it.todo("marks new jobs as seen after they are emailed");
  it.todo("reflects updated job counts in the dashboard stats after a run");
});
