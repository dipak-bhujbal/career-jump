import { describe, it } from "vitest";

describe("e2e signup flow", () => {
  it.todo("renders the signup form and validates required fields");
  it.todo("creates a Cognito account and redirects to the dashboard on success");
  it.todo("shows an inline error when the email is already registered");
  it.todo("shows an inline error when the password does not meet complexity rules");
});
