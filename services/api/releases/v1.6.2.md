Career Jump 1.6.2 keeps the production UI version in sync with the shipped release.

This small follow-up release corrects the footer version label so the app now shows the same version that is actually deployed in production.

## What’s fixed

- The footer version label now matches the real production release.
- The frontend version display is aligned with the packaged app and OpenAPI version for this release.

## Reliability notes

- This is a release-labeling hotfix only.
- Scan behavior, filtering, scheduling, and inventory logic are unchanged from 1.6.1.
